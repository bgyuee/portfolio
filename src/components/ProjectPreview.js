import React from 'react';
import * as s from '../pages/Styled';

function ProjectPreview({project, setProjectOrder, projectPage}) {
    const { title, videos } = project;

  return (
    <s.ProjectPreviewContainer>
    </s.ProjectPreviewContainer>
  )
}

export default ProjectPreview